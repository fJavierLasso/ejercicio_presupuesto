### Setup con Docker

es necesario tener instalados docker y docker-compose.

Simplemente corre:

````
docker-compose up -d
````

y visítala en el puerto 8080.

para cerrar:

````
docker-compose down
````
